<%= form_for(@book) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :name, placeholder: "Add a new book..." %>
	</div>
	<%= f.submit "Add", class: "btn btn-large btn-primary" %>
<% end %>